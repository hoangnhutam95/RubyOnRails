<tr >
            <td style="padding-bottom: 20px; padding-left: 50px"><%= gravatar_for user %> <%= link_to user.name, user %>
            <% if admin_user? %>
            	| <%= link_to "Delete", user, method: :delete, data: {confirm: "Are you sure?"} %>
            <% end %></td>

</tr>